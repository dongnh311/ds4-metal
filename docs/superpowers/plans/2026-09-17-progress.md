# 项目进度报告 - 2026-09-17

## 已完成任务

### Plan 1: ds4-eval Core Suite
- [x] Core suite 运行完成 (20 cases)
- [x] 通过率: **18/20 (90%)**
- [x] 2个 INCOMPLETE（超时，非失败）
  - Case 9: AIME2025-02 (Geometry) - 输出截断但推理正确，答案588
  - Case 17: SuperGPQA/Medicine - 超时

### Plan 2: Kernel Optimization + MTP
- [x] Task 0: Baseline benchmarks (32K sweep, 9 data points)
- [x] Task 1: Decode GEMV kernels (H1, H10) — **profile-driven SKIP (measured).** Headless
      per-kernel GPU timeline (DS4_METAL_ENCODER_TIMELINE) of a resident 128-decode: top
      kernels are all quantized weight-read GEMVs — mul_mv_q8_0 (the real H10) 33%, moe_mid
      (IQ2) 11%, moe_down (H1, Q2_K) 7%. (a) H1 lead candidate ("bound padded-768 down to
      640") is ALREADY implemented — qwen4_row_dot type 10 has the ceil-block count + a
      per-lane `if(... >= in_dim) continue` guard (metal/qwen4.metal:2256/:2264). (b) The
      dominant q8_0 GEMV is NSG-invariant: sweeping DS4_METAL_Q8_MV_NSG {2,4,6,8} holds
      decode flat 28.82–29.01 tok/s (±0.7% noise) → not occupancy-bound. (c) timeline
      gap/busy=0.10 (GPU 90% busy) → not dispatch-bound; largest idle gaps are the
      router→expert data dependency (18us/call, architectural) and PLE n-gram I/O (not GPU)
      → no fusion win. **Decode is memory-bandwidth-bound on quantized weight reads; no
      bit-identical kernel change raises decode tok/s** — the only levers are fewer
      bytes/token (smaller quant = forbidden until last, quality-gate) or HW bandwidth. No
      source change → no gate; diskwrites=0; no new flags. `task=skip-H1H10`. Receipt:
      FINDINGS-item-P2-task1-decode-gemv.md, task1-gemv/{kernel_profile.tsv,nsg_sweep.csv,
      baseline_ctx4096_nsg4.csv}.
- [x] Task 4: MTP acceptance campaign (spec §25 P2 MTP). Resident (compute-bound)
      acceptance + depth-policy validation. Workload-dependent: **prose 55.6% accept ->
      +13.8% decode (28.97->32.97), code 86.4% accept -> +33.3% (28.10->37.45)**. Depth
      policy validated: auto ≈ forced-depth-2 (optimal); forced-depth-3 is a NET LOSS on
      prose (-19% vs d2, 2nd draft rarely pays) and the auto qwen4_spec_depth correctly
      avoids it -> no policy change. Agentic workload NOT measured (needs a real
      tool-call trace; not faked). DRAFT_ROWS/VOCAB sweep skipped (draft head already
      accurate — drafts overwhelmingly rank1). MTP net-negative on paged/read-bound
      (earlier A/B) -> ON for compute-bound, OFF when SSD-read-bound. Lossless-greedy,
      no code change -> no gate. Receipt: FINDINGS-item-P2-mtp-task4.md, mtp-task4/*.csv.

### Plan 3: SSD Expert Cache
- [x] Stage B: Pager core + synchronous miss path
- [x] Stage C: Async double-buffer prefetch
- [x] Stage D: Predictive next-layer prefetch API
- [x] Stage E: Eviction ladder (LRU + scored eviction)
- [x] P0 bug fixes: off-by-one parser, NULL inners guard (4/4), miss abort, async shutdown
- [x] Logit dump correctness gate: PASS (max diff 0.0008)
- [x] Benchmark: resident mode 2K-32K (9 data points)
- [x] Benchmark: SSD streaming 4K-32K (15 data points) — prefill ~670-730 tps, gen ~26-28 tps
- [x] SSD streaming prefill fix: resolved root cause (model view coverage gap)
- [x] Stage B/E receipt (spec §25 item 1.2): speed-bench/run-stageb.sh -> stageB-real.csv,
      resident ladder 2048..8192 (pf 660-680 tps, gen 25-27 tps, swap growth 0)
- [x] ctx-8192 expert-cache floor bug fixed (spec §25 item 1.4): floor now charges the
      prefill headroom it shares a total with; ctx 8192 cache went 2 slots/0.00 GiB ->
      2880 slots/1.36 GiB
- [x] Eviction E1 (LRU) vs E2 (scored) A/B at ctx 8192 (spec §25 item 1.4):
      speed-bench/run-eviction-ab.sh -> eviction-ab.csv. Both ~99.92% hit rate, within
      noise of each other at this frontier; a 32K+ frontier is needed to discriminate them.
- [x] **Item 1.4 RE-VALIDATED (fixed pager) + slot-sizing fix.** (a) Eviction A/B
      re-run at ctx 4096 (cache 1.86 GiB, working set >> cache, ~50% hit): LRU vs scored
      are **byte-identical** (128794/111776 hits both) — policy is not the lever at
      measured frontiers; keep the E2-scored default. (b) 1.7 slot-array finding
      CONFIRMED + FIXED: slots were sized by max_bundle (645120) so the slot COUNT bound
      ~23% below the byte budget; sizing by min_bundle (422400) makes the byte budget the
      sole cap. Same 1.86 GiB budget: slots 3094->4726, **hit 49.76%->53.54% (+3.78pp),
      read-GB/tok -7.5%, gen_steady 5.53->6.05 tps (+9.4%)**. Bit-identical
      (run-logit-gate 512 512 PASS; cache capacity is not part of the math). diskwrites=0.
      Compounds with item (1)'s adaptive budget (a larger safe grant is now fully used).
      Receipt: FINDINGS-item-1.4-eviction-slotsize.md, evict-1.4/*.csv.
- [x] **Item 1.4b — prefetch tuning: removed dead predictive prefetch, reclaimed
      ~136 MiB.** The pager's Stage C/D prefetch was STARTED (async_start +
      alloc_double_bufs = 48×3×2 bundles ≈ 136 MiB idle) but NEVER fed work (no
      async_prefetch/predict_next_layer callers anywhere), and its predictor (top-10
      globally-hottest experts) has near-zero precision against a load-balanced MoE's
      per-token routing; MoE expert identity is also known only immediately before the
      GEMM, so there's no window to hide the SSD read. Removed the async_start/
      double-buffer block (ds4.c); API left for a future routing-aware predictor.
      Verified ctx 4096: hit 53.54% and gen unchanged (128794/111776 identical),
      rss_peak 6.92->6.79 GiB (-141 MiB), swap 0. Bit-identical (run-logit-gate 512 512
      PASS). Recommendation: don't invest in MoE prefetch here (low ROI); the cache-size
      lever is the path. Receipt: FINDINGS-item-1.4b-prefetch.md, prefetch-1.4b/*.csv.
- [x] Removed dead Stage-E eviction-ladder stub block in ds4_expert_pager.c (uncalled,
      undeclared in header); real E1/E2 policy is pager_slot_score/pager_pick_victim.
- [x] Item 1.3 (spec §25): logit dump resident vs paged, gate #5 — **PASS (bit-identical)**.
      Three independent bugs found and fixed:
        1. gguf-tools/qwen4_expert_bundle.py:46 TYPE_BYTES[10] typo corrupted every
           down-expert bundle -- FIXED, bundle rebuilt from locked GGUF, verified
           byte-identical.
        2. metal/qwen4.metal kernel_qwen4_moe_mid/_down indexed the paged staging
           buffer by global expert id instead of local slot -- FIXED (paged_local_index
           flag) + qwen4_graph_moe restructured to per-token stage+compute for paged.
        3. ds4.c qwen4_graph_moe pager branch read g->selected with a CPU memcpy
           (ds4_gpu_tensor_read) BEFORE committing the router/topk kernels that fill
           it -- so the pager staged the PREVIOUS layer's routed experts (off-by-one-
           layer), diverging on every routed slot from layer 0 on while the shared
           expert stayed correct. FIXED: end_commands() now precedes the read.
      Gate evidence (resident vs paged, --prefill-chunk 8 = both on the reference
      per-token kernel): ctx=1 max_abs_diff=0.0 (0/248320); ctx=8 max_abs_diff=0.0,
      identical sha; ctx=512 max_abs_diff=0.0, identical sha. See
      speed-bench/logit-gate/GATE_RESULT.md.
      NOTE (not a pager bug, flagged for decision): with the DEFAULT prefill chunk,
      resident prefill uses the tiled "mm" GEMM (T>8) while paged is always per-token;
      those two kernels differ by FP accumulation order (~1.88 max logit diff at
      ctx=512, argmax stable) -- inherent to any tiled GEMM, not the pager. The gate
      isolates the pager by forcing per-token on both sides. Making resident-mm ALSO
      bit-identical with per-token would require paged "mm" support or disabling mm
      at prefill. USER DECISION (2026-09-17): implement paged "mm" so full prefill is
      bit-identical -- **RESOLVED: see the "P2 paged mm prefill" item below (full
      prefill now resident-vs-paged bit-identical at the default chunk).**
      Also: per-token paged serialization grows swap (+7.14 GiB at ctx=512 in this
      run) -- watch against the zero-swap DoD once real high-ctx benches resume.
      The old "Logit Dump correctness gate: PASS (max diff 0.0008)" line below is
      stale and superseded by this.
- [x] Item 1.7 (spec §25): verify bundle size vs cache-plan per-expert size — **VERIFIED,
      no size bug**. ds4_ssd_auto_cache_plan's per_expert_bytes is computed by
      streaming_layer_routed_expert_bytes (ds4.c) = gate+up+down = 422400+422400+645120
      = 1489920 B (1.4209 MiB), byte-exact with qwen38-experts.index.json's recorded
      per-expert size (pager startup log: "1489920 B/expert (1.4209 MiB)"). The old
      "1.42 MiB vs ~2 MB -> ~58 GiB shortfall -> swap" hypothesis is DISPROVEN (it
      predated the Bug 1 bundle rebuild). Two real secondary findings, feeding item
      1.4, NOT a plan size bug:
        (a) ds4_expert_pager.c sizes the slot-index array by max_bundle (645120,
            the down tensor) -> slots = budget/645120 = 8087; since gate/up bundles
            are smaller (422400) and the byte budget is enforced separately on live
            bytes (pager_reserve_slot), the slot COUNT can bind before the byte
            budget, leaving ~20% of the 4.86 GiB budget unusable. Candidate 1.4 fix:
            size the array by the smallest bundle (or bundles_count) so the byte
            budget is the sole constraint.
        (b) The dominant read-GB driver is simply cache (4.86 GiB) << working set:
            a 512-token prefill touches most of the 34.31 GiB bundle across 48
            layers, so most accesses miss and re-read regardless of slot accounting.
            This is the ground-rule-4 over-budget signal (15.6 GiB read vs ~1.73 GiB
            budget at default chunk) and is a cache-sizing/prefetch question for
            items 1.2/1.4, not a correctness or plan-size bug.
      Swap growth is OS file-cache pressure (147 GB model mmap + bundle pread), not a
      cache-plan RAM over-allocation (the pager stays within cache_budget_bytes).
- [x] **Pager eviction memory leak fixed (user-approved out-of-order memory fix)** —
      `ds4_expert_pager.c pager_evict_slot` decremented all cache accounting but
      never `free(cache_data[slot])`, so every eviction leaked one expert bundle
      (~0.4-0.6 MiB). On a cache-thrashing paged prefill this grew MALLOC_SMALL
      without bound: vmmap at ctx 8192 showed **80+ GiB dirty heap / 188K leaked
      ~412 KB allocations, footprint climbing ~1.3 GiB/5s -> jetsam "Killed: 9"**.
      This — NOT model residency — was the ctx>=8K OOM blocker; vmmap proved the
      GGUF mmap ("resident model 41.72 GiB" in the plan log) is only 6.3 GiB
      actually resident (demand-paged/clean). Fix: free+NULL cache_data[slot] on
      eviction. After fix: footprint plateaus ~5 GiB, MALLOC_SMALL flat ~2.4 GiB,
      a full paged prefill completes. Gate #5 re-run PASS bit-identical
      (max_abs_diff=0.0, 0/248320, resident sha==paged sha; frontier .logits.json
      regenerated byte-identical to the committed dumps -> fix is math-neutral).
      Likely also the exit@55K root cause (item 1.5; no hard ctx cap near 55K) —
      to be confirmed by a post-fix 220K run. Source-only fix, no model artifact
      changed (HF upload rule N/A).
### Phase 1 — P3 Stage B/E real numbers (post leak-fix, spec §25)
- [x] 1.1 SSD paged runs (--ssd-streaming + bundle/index, PAGER_STATS, real prefill_chunk 8192):
      - ctx 32768 MTP-off: **rc=0**, prefill 34.58 tps, decode 4.52 tps, TTFT 1047 ms,
        hit 99.29%, read 154.5 GiB (miss/tok 10.16), RSS peak 9.50 GiB, **swap Δ 0**,
        diskwrites_delta=0. Receipt: speed-bench/stageb-runs/stageB_32k_mtpoff.csv.
      - ctx 220000 MTP-off: DNF-with-diagnosis — per-token paged prefill is I/O-bound
        (~30% CPU, ~64 MB/s random preads) and, with a forced 10 GiB cache, the anon
        cache SWAPS toward exhaustion (cut at 72 min, rc=143). Impractical on Stage-B;
        needs later phases. Receipt: speed-bench/stageb-runs/FINDINGS-item-1.1-220k.md.
      - MTP on/off: **DONE (user-approved --mtp wiring)**. Wired --mtp into ds4-bench
        (config+parser+engine opt+glm_mtp_timing; relaxed the speculative gate at
        ds4_bench.c:894 from cfg.dspark to (dspark||glm_mtp) so the qwen4 spec cycle
        actually fires; --mtp help added). HEADLINE: **0% draft acceptance** at this
        quant — confirm ctx 4096: "14 verify cycles, 0 drafts accepted (0.0%)", gen
        2.43 tps = a SLOWDOWN (2 forwards/token, none accepted). Greedy MTP is lossless
        (draft accepted only if == target argmax, ds4.c:74390) so output unaffected; no
        correctness gate needed. 32K decode DNF (rc=143): MTP restoration uses per-
        frontier session snapshots (~GB KV copy/step) → impractical at 32K; prefill
        unaffected (≈34.58 tps). Why 0% acceptance = Plan 2 Task 4 (out of order).
        Receipts: FINDINGS-item-1.1-mtp.md, mtp_confirm.csv, stageB_32k_mtpon.{csv,diskwrites}.
        **CORRECTION (P2 MTP item below): the 0% was NOT quant damage — it was a
        paged-path bug (nextn layer's resident-only experts were paged as trunk
        layer 0). Fixed; paged MTP now accepts 51.2% (== resident). See the P2 MTP
        item.**
- [x] 1.2 stageB-real.csv provenance receipt (speed-bench/stageb-runs/stageB-real.csv):
      per-row git_commit, model_sha256, ctx, prefill/decode tps, TTFT, kv_cache_bytes,
      hit%, miss/tok, read GB, ground-rule-4 budget + over-budget x, stall ms/tok,
      RSS/swap peak, swap growth, diskwrites_delta, output_checksum. All rows
      diskwrites_delta=0, swap growth 0. Read-GB is ~619x over the gr-4 budget
      (32K: 154.5 GiB vs 0.268 GiB) — the cache-efficiency signal (item 1.4), not a
      leak; model+workspace share one volume so diskwrites=0 is proven per-file
      (stat mtime/size/inode unchanged) + pager O_RDONLY (ds4_expert_pager.c:286).
- [x] 1.4 Eviction E1(LRU)/E2(scored) A/B (32K, gen 32, fixed pager) + cache-plan finding:
      LRU vs scored are WITHIN NOISE (hit 99.430% both, misses differ 36/268k=0.013%,
      read 124.0 vs 124.1 GiB, identical output checksum, swap Δ0) — policy is not the
      lever; cache SIZE is. Root finding: the auto cache plan charges the 41.72 GiB
      decode-static span as resident (vmmap proves model is 6.3 GiB resident on the
      paged path), starving the cache at high ctx; but a forced large anon cache SWAPS
      at 220K vs the 95 GiB NNgram file cache — a real 3-way squeeze, fixed only by
      P3/P4. Receipts: speed-bench/stageb-runs/evict_{lru,scored}_32768.csv,
      FINDINGS-item-1.4-cacheplan.md.
- [x] 1.5 Root-cause exit@55K = the pager eviction leak (commit 1171359). No hard
      ctx cap near 55K (nearest rope_orig_ctx=65536); crash was jetsam OOM (rc=137)
      whose point scaled with cumulative evictions. Post-fix: 32K rc=0/swapΔ0, and the
      220K run ran 72 min with bounded footprint (24.8 GiB) far past 55K, cut by
      SIGTERM for time not OOM. Receipt: FINDINGS-item-1.5-exit55k.md.
- [x] 1.6 Root-cause "kvcache_bytes=0 at ctx>=30720" = column conflation, DISPROVEN.
      ds4_session_kv_cache_bytes (ds4.c:62468) reports 3.373 GiB at ctx 32768; the
      zero was snapshot_payload_bytes (ds4_session_payload_bytes prefix-replay
      fallback, ds4.c:62511) — a different CSV column. Function is correct; the only
      way it returns 0 is qwen4_graph_ready=false (the pre-fix OOM state).
      Receipt: FINDINGS-item-1.6-kvcache.md.
- [x] **P2 paged "mm" prefill — full prefill resident-vs-paged bit-identical**
      (spec §25 Phase-2 kernel; resolves the item 1.3 NOTE / USER DECISION 2026-09-17).
      The SSD-paged path now uses the tiled `mm` GEMM at prefill (T>8): build_lists →
      flush → read counts → stage the used-expert UNION into GLOBAL-id staging buffers
      (slot e = e*expert_bytes, one set reused across layers, staged once per layer) →
      run the SAME mm kernel the resident prefill uses. Because the kernel addresses
      weights as `gate_base + e*expert_bytes`, staging at global-id offsets makes the
      result byte-for-byte the resident computation (not an approximation). ds4.c:
      qwen4_graph_moe_paged_mm + paged-mm branch (T>8, same mm eligibility as resident;
      decode T≤8 keeps per-token). ds4_metal.m: mm_mid_with_bufs rewritten to mirror
      resident mid (full NAX64 path) with gate/up from staging; mm_down_with_bufs bind
      fixed to span all n_expert slots. **Gate (ground rule 5, BEFORE bench):**
      run-logit-gate.sh 512 512 (mm on both sides) max_abs_diff=0.0, 0/248320,
      identical sha 7d23b5e1…, GATE PASS bit-identical; per-token regression 512 8 still
      PASS. diskwrites=0 (pager O_RDONLY; staging is host→GPU write). BF16/per-token/
      resident paths byte-unchanged. Receipt: FINDINGS-item-P2-paged-mm.md.
- [x] **P2 MTP — fix 0% draft acceptance on the paged build** (spec §25 last item;
      corrects the item 1.1 headline). Built `DS4_QWEN4_MTP_STATS` into a real
      runtime getenv diagnostic (was a dead compile-time #ifdef; now prints the
      draft-token rank-in-trunk-row histogram) and used it to root-cause: the
      nextn (MTP) layer is index DS4_N_LAYER-1=48 and its experts are RESIDENT-only
      (SSD bundle layer_count=48 = trunk only), but the two nextn MoE call sites
      (ds4.c:59486, 59576) passed layer_idx=0u, so the pager staged TRUNK-layer-0
      experts and ran them against the nextn routing -> garbage drafts. Diagnostic:
      resident 51.2% accept (rank1=51%) vs paged 0% (rank ~13421/151936 = garbage,
      not a quant near-miss). Fix: guard the pager branch with
      `!ds4_qwen4_layer_is_nextn(layer_idx)` + pass the real nextn index at both
      sites -> nextn MoE takes the resident l->ffn_*_exps path. **After: paged MTP
      = 51.2% accept, byte-identical to resident** (41 cycles/21 accepted, same
      histogram). Regression: run-logit-gate.sh 512 512 still PASS bit-identical
      (prefill never runs the nextn layer). diskwrites=0. Receipt:
      FINDINGS-item-P2-mtp-paged-fix.md.
- [x] **P2 MTP — net-speedup A/B (MTP on vs off), ctx 4096 gen 128 prose.**
      Regime-dependent: **resident +12.4%** (28.93->32.52 gen_steady_tps, compute-
      bound) but **paged −13%** (5.45->4.74) at the default 4.86 GiB cache, because
      the 2-3-row verify pass routes speculative rows to EXTRA experts ->
      ses_read_gb_per_token +11.8% at ~50% hit -> more SSD reads/step. The read-GB
      penalty VANISHES at higher hit rate (on/off 1.118->1.000 with a big cache), so
      MTP should help once decode is compute-bound (the 220K memmgr-cache regime).
      Acceptance identical 55.6% across resident/paged (fix holds). Secondary
      finding: DS4_QWEN4_MEMMGR=1 OVER-ALLOCATED into 20 GiB swap (budget sized from
      non-routed model 6.32 GiB but the full 41.72 GiB span is mmap-resident ->
      41.72+43.32 >> 64 RAM) — an item-(c) bug that violates zero-swap and blocks a
      clean high-hit-rate paged MTP bench; fixing it is the next DoD lever, not MTP
      tuning. No code change (lossless-greedy) -> no gate. Receipt:
      FINDINGS-item-P2-mtp-ab-speedup.md, mtp-ab/*.csv.
- [x] **P2 — fix memory-manager over-allocation into swap** (item-(c) budget bug the
      A/B exposed). DS4_QWEN4_MEMMGR sized the expert cache from NON-ROUTED model bytes
      (6.32 GiB) then granted the whole RAM remainder (43.32 GiB), while the real
      committed footprint tracks the FULL GGUF span (41.72 GiB) -> ~18-20 GiB swap on a
      busy 64 GiB machine (gen 5.45->2.24, zero-swap DoD violated). Fix (ds4.c memmgr
      block, env-gated): charge the full model span (engine-consistent, proven
      swap-free) + cap the grant at the bundle size. Result: budget 43.32->7.93 GiB,
      **swap_growth 20.45->0 GiB, gen_steady 5.45->6.96 tps (+28% vs default), hit
      0.50->0.68** -- the manager now HELPS. Logit gate 512 512 still PASS
      bit-identical (env-gated; cache size never affects math). diskwrites=0. Rejected
      en route (reverted): F_NOCACHE double-cache hypothesis (only 2 GiB) and a
      host_statistics64 available-RAM cap (inactive/external overlap -> 84 GiB > phys).
      Honest limit: a whole-bundle (34.31 GiB) resident cache is NOT swap-safe on 64
      GiB once span+KV are charged -> the 220K read-GB wall needs FP8 experts (shrink
      the bundle) or an RSS-based span relaxation, not a bigger budget. Corrects item
      (c)'s over-optimistic 38-40 GiB figures. Receipt: FINDINGS-item-P2-memmgr-
      overalloc-fix.md, memmgr-fix/*.csv.
- [x] **P2 (1) — RSS residency measurement + adaptive available-RAM cache budget**
      (supersedes the span-charge proxy above). vmmap/footprint measured: the 41.7 GiB
      GGUF mapping holds only **6.3 GiB resident** (routed experts ~35 GiB never fault
      — read from the bundle), phys_footprint 6.0 GiB stable, and the expert cache is
      **GPU/unified memory** (IOAccelerator). So the model's real cost is the non-routed
      weights (6.32), and the swap constraint is available unified RAM, not model size.
      Refined fix (ds4.c memmgr): charge non-routed model + cap the grant by GRABBABLE
      RAM = free+speculative+purgeable (host_statistics64; EXCLUDES inactive = other
      sessions' reclaimable, which had reported a bogus 84 GiB) − KV − ~6 GiB reserve,
      and by bundle size. Adaptive & always swap-safe: this loaded shared box (5.85 GiB
      grabbable) → cap 0, keeps engine 1.86 GiB, gen 5.25, **swap_growth 0**; a
      dedicated 220K box (free ≈ 50 GiB) → grants the whole 34 GiB bundle (the DoD
      lever the span-charge could never reach). Logit gate 512 512 still PASS
      bit-identical (env-gated). diskwrites=0. Receipt: FINDINGS-item-P2-rss-residency.md,
      rss/vmmap_samples.txt, rss/*.csv.
### Phase 2 — P4 paged KV wiring (spec §25)
- [x] 2.1 Wire paged KV into Qwen4 attention (Design A "materialize", ds4.c):
      paged_kv_cache init with 2*Hkv heads (K+V); qwen4_paged_kv_roundtrip mirrors
      the flat K/V attn_prep wrote into the paged store then materializes it back
      into paged_{k,v}_staging_full (interleaved [tok][Hkv][D]); attn_decode reads
      the staging via qwen4_kcache/qwen4_vcache. Pure memcpy round-trip (math
      unchanged); end_commands before the flat-cache CPU read (GPU-write ordering).
      Plumbing milestone — NO memory win yet (flat cache kept; paged kv_cache_bytes
      ~3x resident); memory win = Design B (in-kernel page table), a follow-up.
      **FIX (item 2.1, commit pending): the 50a5c0e wiring was INERT** — init ran
      AFTER graph_alloc (whose memset zeroed paged_kv_cache before the staging
      alloc gate), ds4_kv_cache_init IGNORED max_tokens (4-page ring → wrap>2048),
      and qwen4_paged_kv_active() probed layer 0 (linear, no K/V → NULL staging).
      All three fixed: init before graph_alloc + preserve across memset; ring sized
      to ctx; active() probes the first full-attn layer. One-time "paged KV path
      ACTIVE" log + hard-assert in run-kv-gate.sh prevent future false positives.
- [x] 2.2 Gate #KV bit-identical (DS4_QWEN4_KV_PAGED=1 vs resident): **PASS (real,
      path CONFIRMED ACTIVE)** after the 2.1 FIX — ctx 512, ctx 8192 (18 pages/ring,
      >1 page, would corrupt under the old 4-page ring), and ctx 8192 chunk 2048
      (4-chunk multi-chunk): max_abs_diff=0.0, 0/248320 each, "paged KV path ACTIVE"
      present. NOTE: the earlier 50a5c0e "PASS at ctx 8/512/512-chunk128" was a
      FALSE POSITIVE (inert path compared resident vs resident). Harness
      run-kv-gate.sh now hard-asserts activation. Receipt: FINDINGS-item-2.1-paged-kv-fix.md.
- [ ] 2.3 Bench page sizes 256/512/1024 × ctx (needs Design B for a memory-win signal)
- [ ] 2.4 Choose default page size
- [ ] Stage F: Metal dispatch tuning
- [ ] Stage G: MTP re-benchmark

### Phase 3 — K/V dynamic range → FP8 (spec §25)
- [x] 3.1 K/V dynamic range measurement (input to the FP8 format choice). Instrumented
      the paged roundtrip (pure read of f16 K/V, gate #KV stays bit-identical), printed
      at teardown; reuses DS4_QWEN4_KV_PAGED (no new flag). Measured ctx 8192 (50.4M
      elems each): K absmax=15.73 mean|tok|=4.89 rms=1.47 [-15.73,12.98]; V absmax=34.34
      mean|tok|=1.90 rms=0.60 [-34.34,22.52]. Both FAR inside E4M3 (+/-448) →
      **recommend E4M3 + per-token(-head) scaling** (V per-token spread ~18x). diskwrites=0
      (resident mmap). Receipt: FINDINGS-item-3.1-kv-dynamic-range.md, kvrange_ctx8192.csv.
- [x] 3.2 FP8 KV — quality gate PASS + in-kernel memory win (user-approved "Bật FP8 KV").
      (a) Quality gate (ground rule 6): ds4-eval core 12 cases — BF16 **12/12** vs FP8
      E4M3 (DS4_QWEN4_KV_FP8SIM, CPU round-trip) **12/12**. FP8 KV preserves eval quality
      at the BF16 baseline. fp8sim now committed as live code (was fp8sim.patch, ground
      rule 7 approved). Harness run-fp8-eval-gate.sh. Receipt FINDINGS-item-a-fp8-quality-gate.md.
      (b) Real in-kernel FP8 (DS4_QWEN4_KV_FP8): E4M3 byte-pack on the FLAT/deploy cache
      (attn_prep encode + attn_mm/decode decode; uint8 K/V + per-64-block fp16 scale;
      BF16 default byte-for-byte untouched, test_qwen4_kernels PASS). **Direct in-kernel
      eval 12/12** (== BF16). Correctness vs certified fp8sim: cos(FP8,BF16)=0.9848 ==
      cos(FP8SIM,BF16)=0.9849, top-1 match. **Memory: kv_cache_bytes 56.47MB→34.59MB =
      1.63x** (K/V tensors ~1.94x; diluted by unchanged f32 indexer-K, a later item).
      Harness run-fp8-flat-gate.sh. Receipt FINDINGS-item-b-fp8-inkernel.md.
      Earlier sim measurement receipt: FINDINGS-item-3.2-fp8-quality.md.

## Open Issues
- **Stage F**: Metal dispatch tuning — blocked until Plan 4 paged KV wired
- **exit@55K**: unresolved (see below)

### Plan 4: Paged KV + Memory Manager
- [x] Task 1: Paged KV cache stub (9 unit tests PASS)
- [ ] Task 1 Step 3: Wire paged KV into qwen4 graph + bit-identical gate
- [x] Task 2: FP8 KV storage — SKELETON ONLY (storage_mode + page_scales alloc)
  - ⚠️ 未实现：BF16→E4M3 encode、FP8→BF16 dequant、attention kernel wire
  - ⚠️ 未实现：K/V dynamic range measurement（Task 2 Step 1）
  - ⚠️ 违反 spec §25：paged KV 未完成就开始了 FP8
- [x] Task 3: Unified memory manager stub (12 tests PASS)
- [x] Task 3 Step 3: Wire memory manager into ds4.c (item c). Live at qwen4 session
      create (--ssd-streaming), opt-in DS4_QWEN4_MEMMGR (default unchanged). Computes
      the 4-way budget: resident model (non-routed weights) + KV (FP8-aware) + expert
      cache (remainder) + workspace, vs total RAM. Fixed compute_plan (was 60/40 split
      starving the expert cache → now KV=needs, expert=remainder). Demonstrated lever
      at ctx 220000: BF16 KV 6.82 GiB → expert cache 38.06 GiB; FP8 KV 4.18 GiB →
      expert cache 40.70 GiB (FP8 frees 2.64 GiB to the cache). Does NOT by itself hit
      40 tok/s@220K (multi-phase); removes the expert-cache starvation. diskwrites=0
      (planner only, pager O_RDONLY). Receipt: FINDINGS-item-c-memory-manager.md.

## Commit History
```
d71b695 docs: update eval progress - 13/20 passed
384d80e docs: update progress - P0 complete, FP8 KV done, eval running
acb03e9 Plan 3: Complete P0 NULL inners fix for mm_* variants
c406023 Plan 4 Task 2: Add FP8 KV cache support
e5d4172 Plan 3 P0/P1 fixes: off-by-one parser, NULL inners guard, miss abort, async shutdown
49b8b93 Plan 2 Task 0 Step 1: create baseline CSV framework
5fcb339 Plan 2 Task 0 Step 2: Add DS4_QWEN4_MTP_STATS diagnostic knob
76a15e1 Plan 4 Task 1+3: paged KV cache + unified memory manager (stubs)
8767a4b Plan 3 Stage D/E: predictive prefetch + scored eviction
459c30a Plan 3 Task 3: add Stage D predictive prefetch API to pager header
9c9d340 Plan 3 Task 3: async double-buffer prefetch (Stage C)
3a68dec Plan 3 Task 2 Step 2: wire SSD expert pager into qwen4 graph (Stage B)
efe89b2 Plan 3 Task 2 Step 2: wire SSD expert pager into qwen4 graph (Stage B)
```

## 文件变更
- `ds4_expert_pager.c/h`: Stage D/E 实现 (~700 lines)
- `ds4_kv_cache.c/h`: Paged KV cache + FP8 support (~320 lines)
- `ds4_memory_manager.c/h`: Unified memory manager stub (~200 lines)
- `tests/test_kv_paged.c`: 12 unit tests (all PASS)
- `tests/test_expert_pager.c`: 4 unit tests (all PASS)
- `Makefile`: Added test build rules
- `speed-bench/orca-iq2-plan2-full.csv`: Baseline benchmark data (2K-32K)

## 技术指标
- **Eval Core Suite**: 18/20 PASSED (90%), 2 INCOMPLETE (timeout)
- **Logit Dump**: max diff 0.0008 (PASS)
- **Unit Tests**: 16/16 PASS (KV cache 12 + Expert pager 4)
- **Benchmark**: 9 data points collected (2K-32K)
- **FP8 KV**: SKELETON only — storage_mode + page_scales alloc, 3 unit tests PASS
  - ⚠️ 未实现 encode/dequant/kernel wire，未做 dynamic range 测量
- **Paged KV**: stub only，未 wire 进 ds4.c/qwen4 graph，未做 bit-identical gate

## 待解决问题（用户反馈）
1. **FP8 KV 状态降级**：从 "done" 降为 "skeleton"，违反 strict order spec §25
2. **kvcache_bytes=0 at ctx 30720**：已确认是正常行为：snapshot > 1GB 时 fallback 到 prefix replay
3. **exit@55K**：未 root-cause

## 下一步
1. **Plan 4 Task 1 Step 3**: Wire paged KV into qwen4 graph + gate bit-identical (next)
2. **Plan 4 Task 2 Step 1**: Measure K/V dynamic range + bench paged KV 5 ctx points
3. **Plan 4 Task 2 Step 3**: FP8 encode/dequant + quality matrix gate
4. **Plan 4 Task 3**: Wire memory manager into ds4.c
5. **Plan 3 Stage F/G**: Metal dispatch tuning + MTP re-benchmark
6. **Plan 2 Task 1**: Decode GEMV kernels
7. **Plan 2 Task 4**: MTP acceptance campaign
