We need to respond to user: "Write a Python function to compute the nth prime number, with a docstring, type hints, and a brief complexity note." Need produce final answer likely code. Need decide function. Could use simple trial division or sieve. Need docstring, type hints, complexity note. Maybe robust: nth prime (1-indexed). Complexity note. Could implement efficient enough: use upper bound theorem for nth prime and sieve of Eratosthenes. Complexity O(n log log n) roughly with bound. But if n small? Need compute nth prime. Could use simple trial division O(n sqrt p_n) maybe. Better: sieve with upper bound. Need type hints. Maybe include import math. Function nth_prime(n: int) -> int. Validate n positive.
