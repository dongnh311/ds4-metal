Write a detailed, well-structured technical explanation of how speculative decoding accelerates autoregressive large language model inference. Cover: the draft model proposing candidate tokens, the target model verifying them in parallel, the accept/reject sampling criterion that preserves the target distribution, multi-token blocks, and why the wall-clock speedup depends on the acceptance rate and the relative cost of draft versus verify. Then explain how Multi-Token Prediction (MTP) heads differ from a separate draft model. Be thorough and precise.
