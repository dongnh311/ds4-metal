greedy 128-tok temp0, imat, tensor ON vs OFF: OUTPUTS DIVERGE (both coherent, different token paths from the first reasoning block)
3c3
< Need produce final answer thorough. Need think carefully. Need include technical details: speculative decoding algorithm (Leviathan et al. 2022, Chen et al. 2023). Autoregressive LM inference: target model p_t(x_t | x_<t). Draft model q(x_t | x_<t) proposes gamma tokens. Verification: target computes probabilities for positions t..t+gamma in parallel (one forward pass with batched sequence? Actually target processes prefix plus draft tokens to get p_t(x_t), p_t(x_{t+1}), ..., p_t(x_{t+gamma})? Need careful: For speculative decoding, draft generates gamma tokens x_{t+1}...x_{t+gamma}. Target evaluates likelihoods for these tokens conditioned on prefix and previous draft tokens? It computes p_t(x_{t+1}|x_<t), p_t(x_{t+2}|x_<t, x_{t+1}), ..., p_t(x_{t+gamma}|x_<t, x_{t+1}...x_{t+gamma-1}) in one forward pass because target is autoregressive and can process sequence of draft tokens in parallel with causal mask. Then acceptance: for i=1..gamma, accept x_{t+i} with probability min(1, p_t(x_{t+i}|...)/q(x_{t+i}|...)) if q is proposal distribution. If reject, sample residual token from adjusted distribution (p_t - q)_+ normalized at that position, then discard rest. If all accepted, optionally sample one extra token from target distribution at next position (or accept? In original algorithm, if all gamma accepted, sample x_{t+gamma+1} from p_t(x_{t+gamma+1}|prefix+accepted draft tokens)). This yields exact target distribution if q known and target probabilities exact. Expected accepted tokens per verification pass: gamma + 1? Let's derive speedup. Let alpha = acceptance probability per token? If independent identical acceptance prob alpha, expected accepted tokens before rejection = sum_{i=1}^gamma alpha^i? Actually number accepted before rejection: if reject at i, accepted i-1; if all accepted, gamma plus bonus token. Expected tokens generated per target forward = E[accepted] + 1? Let's define. In speculative decoding, each target forward verifies gamma draft tokens and may produce up to gamma+1 tokens (gamma accepted + bonus if all accepted). Expected number of tokens per target forward = 1 + sum_{i=1}^gamma alpha^i? Wait if all gamma accepted, bonus token generated. If reject at i, accepted i-1 and residual token generated at rejection position. So tokens generated = i if reject at i (i-1 accepted + residual), or gamma+1 if all accepted. Probability reject at i = alpha^{i-1}(1-alpha) for i=1..gamma, all accepted alpha^gamma. Expected tokens = sum_{i=1}^gamma i alpha^{i-1}(1-alpha) + (gamma+1) alpha^gamma. Simplify = 1 + sum_{i=1}^gamma alpha^i? Let's check: For gamma=1: expected = 1*(1-alpha)+2*alpha = 1+alpha. Formula 1+alpha yes. For gamma=2: reject1:1*(1-alpha); reject2:2*alpha(1-alpha); all:3 alpha^2 => 1-alpha+2alpha-2alpha^2+3alpha^2=1+alpha+alpha^2. yes. So expected tokens per target forward = 1 + sum_{i=1}^gamma alpha^i. If alpha=1, gamma+1. Speedup relative to target-only: target-only one token per forward. Speculative uses one target forward plus gamma draft forwards. If draft cost c_d relative to target c_t, speedup = expected tokens per target forward / (1 + gamma * c_d/c_t) maybe. If draft negligible, speedup ~ expected tokens per target forward. If draft not negligible, net speedup = E[T]/(1+gamma*r) where r = draft forward cost/target forward cost. Also target forward with gamma+1 tokens may cost more than single token due to compute? On memory-bound, target forward cost roughly constant for small batch? Need mention.
---
> Need produce final answer thorough. Need think carefully. Need include technical details: speculative decoding algorithm (Leviathan et al. 2022, Chen et al. 2023). Autoregressive LM inference: target model p_t(x_t | x_<t). Draft model q(x_t | x_<t) proposes gamma tokens. Verification: target computes probabilities for positions t..t+gamma in parallel (one forward pass with batched sequence? Actually target processes draft tokens as input to get logits for each position). For each proposed token x_{t+i}, accept with probability min(1, p_t(x_{t+i}|prefix)/q(x_{t+i}|prefix)). If accepted, continue; if rejected, sample residual token from modified distribution: p'(x) = (p_t(x) - q(x))_+ / sum_y (p_t(y)-q(y))_+ for that position, then discard rest. This yields exact target distribution if q known and target probabilities exact. If q is deterministic greedy? For deterministic decoding, accept if argmax matches? But standard stochastic. Need mention batch proposal: draft model autoregressively generates gamma tokens, maybe using its own KV cache; can be small model, self-drafting, n-gram, etc. Candidate tokens proposed in batch: draft model produces sequence x_{t+1}...x_{t+gamma}; target model verifies in one forward pass by feeding these tokens (teacher forcing) and computing logits at each position. Need mention target forward pass cost: one pass over gamma+1 tokens? Actually to verify gamma tokens, target needs logits for positions t..t+gamma-1? If current prefix length t, draft proposes tokens y_1..y_gamma. Target forward on prefix plus y_1..y_gamma? It computes logits for y_1..y_gamma and maybe next token after accepted. If all accepted, need sample next token from target at position t+gamma? Could require extra forward or use logits from last proposed? Let's clarify: Suppose prefix x_1..x_t. Draft proposes x_{t+1}..x_{t+gamma}. Target computes p(x_{t+1}|x_1..x_t), p(x_{t+2}|x_1..x_{t+1}), ..., p(x_{t+gamma}|x_1..x_{t+gamma-1}) in one forward pass over sequence x_1..x_t,y_1..y_gamma? It also can compute p(x_{t+gamma+1}|x_1..x_{t+gamma}) if input includes y_gamma? Actually logits at position t+gamma correspond to next token after y_gamma, if input sequence includes y_1..y_gamma. But to verify y_gamma, need p(y_gamma | prefix + y_1..y_{gamma-1}), which is logits at position t+gamma-1. If all accepted, we can use logits at position t+gamma to sample next token without extra forward? Yes if target forward includes y_gamma as input, output at last position gives distribution for next token. So one target forward can verify gamma and produce next token if all accepted. If reject at i, use logits at position t+i-1? Need indexing.
