Explain, in careful technical detail, how a modern speculative-decoding scheme accelerates autoregressive language-model inference. Cover the roles of the draft model and the target model, how candidate tokens are proposed in a batch, how the verification step accepts or rejects each proposed token using the target model's probabilities, and why the expected number of accepted tokens per verification pass determines the overall speedup. Then discuss how a multi-token-prediction (MTP) head changes this picture, how key-value cache reuse interacts with rejected tokens, and what numerical-precision pitfalls can cause the draft and target distributions to diverge. Conclude with a short paragraph on how batching several independent decoding sessions together affects throughput versus latency on memory-bandwidth-bound hardware such as Apple Silicon.
